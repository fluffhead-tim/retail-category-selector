run = "bash -lc 'pip install -r requirements.txt && gunicorn -k gthread -w 2 -b 0.0.0.0:$PORT src.api.main:app'"
modules = ["python-3.12", "nix"]

[deployment]
run = ["sh", "-c", "bash -lc 'pip install -r requirements.txt && gunicorn -k gthread -w 2 -b 0.0.0.0:$PORT src.api.main:app'"]

[nix]
channel = "stable-25_05"
packages = ["libiconv", "libxcrypt", "rapidfuzz-cpp", "taskflow"]

[[ports]]
localPort = 5000
externalPort = 80

[[ports]]
localPort = 8000
externalPort = 8000

[[ports]]
localPort = 37317
externalPort = 3000

[agent]
expertMode = true

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "API Server"

[[workflows.workflow]]
name = "API Server"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "PORT=8000 python -m src.api.main"
waitForPort = 8000

[workflows.workflow.metadata]
outputType = "console"
